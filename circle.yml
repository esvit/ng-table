machine:
  node:
    version: 6.3.1

dependencies:
  pre:
    - npm install -g karma-cli
    - npm prune && npm install
    - npm run build
  cache_directories:
    - node_modules

test:
  override:
    - npm test